import { styled } from 'stitches.config';

export const SignInButtonText = styled('Text', {
  fontSize: 14,
  lineHeight: 20,
  marginLeft: 12,
  fontFamily: '$bold',
  color: '$blackPrimary',
  textTransform: 'uppercase',
});
