import { styled } from 'stitches.config';

export const ButtonText = styled('Text', {
  fontSize: 14,
  lineHeight: 20,
  marginLeft: 12,
  fontFamily: '$bold',
  color: '$white',
  textTransform: 'uppercase',
});
